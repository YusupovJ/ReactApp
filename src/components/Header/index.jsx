import React from "react";
import { HeaderStyle } from "./style";
import { rem } from "@functions";

const Header = (props) => {
	return <HeaderStyle className="header">Header</HeaderStyle>;
};

export default Header;
